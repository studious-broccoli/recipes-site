[project]
name = "ariannas-kitchen-backend"
version = "0.1.0"
description = "FastAPI backend for Arianna's Kitchen recipe site"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
    "pydantic>=2.7.0",
]
